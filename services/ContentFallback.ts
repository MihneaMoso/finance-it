/**
 * ContentFallback — temporary local fallback content.
 *
 * Used when Firestore is unavailable or empty.
 * TODO: remove once Firestore seed + offline caching are solid.
 */

export const FALLBACK_ENABLED = true;
